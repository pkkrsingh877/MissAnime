<%- layout('./layouts/boilerplate') %>

<h1 class="title">Admin Panel</h1>

<ul>
    <% for( let index = 0; index < news.length; index++ ) { %>
        <li>
        <a href="/articles/<%= news[index]._id %>"><%= news[index].title %> </a>
        </li>
    <% } %>
</ul>